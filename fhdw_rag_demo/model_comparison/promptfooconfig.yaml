# yaml-language-server: $schema=https://promptfoo.dev/config-schema.json
description: 'FHDW RAG Demo - Modellvergleich'

# API-Keys werden aus .env geladen (symlink)

prompts:
  - file://prompt_naive.txt
  - file://prompt_robust.txt

providers:
  - id: openai:gpt-3.5-turbo
    label: 'GPT-3.5-Turbo (schwächer)'
    config:
      temperature: 0.3
      max_tokens: 500
  - id: openai:gpt-5.2
    label: 'GPT-5.2 (Aktuell)'
    config:
      temperature: 0.3
      max_tokens: 500

tests: file://tests.yaml
